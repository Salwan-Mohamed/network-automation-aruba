---
# Static inventory file
# This file can be used for static switch definitions if needed
# The main playbook uses dynamic inventory based on room mappings

aruba_switches:
  hosts:
    # Example static entries (uncomment and modify as needed)
    # switch-ag2:
    #   ansible_host: 10.1.127.248
    #   switch_model: 2930F
    #   location: "AG Floor"
